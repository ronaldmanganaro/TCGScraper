name: Docker Image CI

on:
  push:
    branches: 
      - '*'
  
jobs:

  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag tcgpricetracker:$(date +%s)
